services:
  monitor:
    build: .
    container_name: multica-weixin-monitor
    network_mode: host
    env_file:
      - .env
    volumes:
      - /run/docker.sock:/run/docker.sock:ro
    restart: unless-stopped
